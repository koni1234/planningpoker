input CreateUserInput {
    name: String!
}

type User {
    id: ID!
    name: String!
    vote: String
}

type Query {
    getUser(id: ID!): User
}

type Mutation {
    createUser(createUserInput: CreateUserInput!): User
}